import React from 'react';
import renderer from 'react-test-renderer';

import TimeBtn from '../../components/TimeBtn';

// Tests that parseTime function returns correct string for single digit time
it('test that parseTime function transforms time correctly', () => {
  
  
});